---
title: Overview
description: riblt is the reconciliation engine; set-sync is the protocol layer built on top of it.
---

This workspace is split into two layers:

- `riblt`: a low-level TypeScript implementation of Rateless Invertible Bloom Lookup Tables.
- `set-sync`: a JSON-first client/server protocol that uses `riblt` frames to reconcile two sets.

Use `riblt` directly if you need raw sketch generation and merge/decode control. Use `set-sync` if you want a higher-level session workflow between a client and server.

## Packages

<Cards>
  <Card title="riblt" href="/docs/riblt" />
  <Card title="set-sync" href="/docs/setsync" />
</Cards>

## Workspace Layout

```text
packages/
  riblt/      core reconciliation engine
  setsync/    protocol wrapper around riblt
examples/
  browser-server/   browser + Node demo
docs/
  content/docs/     this documentation site
```

## Typical Flow

1. Each side starts with a local set of string identifiers.
2. `riblt` turns that set into coded symbols that can be streamed incrementally.
3. `set-sync` wraps those coded symbols in session messages like `hello`, `sync`, and `sync-response`.
4. Both sides keep exchanging frames until the diff can be decoded.
