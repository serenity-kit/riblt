{
  "name": "riblt-workspace",
  "private": true,
  "scripts": {
    "build": "pnpm -r --filter './packages/*' build && pnpm --filter browser-server-example build",
    "test": "pnpm -r --filter './packages/*' test",
    "lint": "pnpm -r --filter './packages/*' lint && pnpm --filter browser-server-example lint",
    "example": "pnpm --filter browser-server-example start"
  },
  "devDependencies": {
    "@types/node": "^25.2.0",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
